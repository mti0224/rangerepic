# RangerEpic Gameplay Data

這裡是 RangerEpic 自有的遊戲資料，不是 LINE Rangers 原始戰鬥資料。

- `characters/`：角色本體（熊大、兔兔等）
- `classes/`：角色職業，包含新 Stats、普通攻擊、普通輔助、技能、能力
- `rules.json`：全域 Battle Rules v1

動畫、SAM、彈道、Anchor、Cutin 等視覺資料仍保留在 `public/rangers/<assetVariantId>`。
